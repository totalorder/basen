version: '2.1'
services:
  app:
    build: ./dependencies/app
    ports:
      - 8080

  postgres:
    build: ./dependencies/postgres
    ports:
    - 5432

  postgres-migrate:
    build: ./dependencies/postgres
    ports:
    - 5432